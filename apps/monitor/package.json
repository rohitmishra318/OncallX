{
  "name": "@oncallx/monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Synthetic health-check monitor for OnCallX — checks external targets and fires alerts via the OnCallX API.",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "axios": "^1.7.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
